import { Text } from 'react-native';
import styled from 'styled-components';

const CommonError = styled(Text)`
    margin-top: 1;
    margin-bottom: 3;
    margin-left: 15;
    color: red;

`;

export default CommonError;